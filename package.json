{
  "name": "bhop",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Johan Liebert",
  "license": "MIT",
  "scripts": {
    "build": "docker-compose --env-file=.env.dev up -d --build",
    "run": "docker-compose --env-file=.env.dev up -d",
    "stop": "docker-compose --env-file=.env.dev down"
  },
  "dependencies": {
    "gitignore": "^0.7.0"
  }
}
